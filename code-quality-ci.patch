diff --git a/.github/workflows/code-quality.yml b/.github/workflows/code-quality.yml
index 3136442..d440dde 100644
--- a/.github/workflows/code-quality.yml
+++ b/.github/workflows/code-quality.yml
@@ -23,11 +23,13 @@ jobs:
       - name: Lint
         uses: astral-sh/ruff-action@v4.1.0
         with:
+          version: "0.16.6"
           args: check --output-format=github
       - name: Format check
         uses: astral-sh/ruff-action@v4.1.0
         continue-on-error: true
         with:
+          version: "0.16.6"
           args: format --check --diff
 
   codeql:
diff --git a/.github/workflows/qa.yml b/.github/workflows/qa.yml
index aafc868..8311fb9 100644
--- a/.github/workflows/qa.yml
+++ b/.github/workflows/qa.yml
@@ -1,12 +1,10 @@
 name: Run notebooks and validate the results
 
+# NOTE: This workflow requires NVIDIA's self-hosted GPU runner fleet
+# (arc-runners-org-nvidia-ai-bp-4-gpu) and NIM API secrets that are not
+# available in this fork, so the automatic push/PR triggers are disabled.
+# It can still be started by hand from the Actions tab if those become available.
 on:
-  push:
-    branches:
-      - main
-  pull_request:
-    branches:
-      - main
   workflow_dispatch:
 
 concurrency:
diff --git a/pyproject.toml b/pyproject.toml
index 7b73738..1f3c6c5 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -52,3 +52,10 @@ dev-dependencies = [
   "pytest-asyncio==1.3.0",
   "httpx==0.28.1",
 ]
+
+[tool.ruff]
+target-version = "py311"
+
+[tool.ruff.lint]
+# Pin an explicit rule set so a new Ruff release can't change what CI enforces.
+select = ["E4", "E7", "E9", "F"]
diff --git a/src/backend/vlm.py b/src/backend/vlm.py
index 007cd9f..a970559 100644
--- a/src/backend/vlm.py
+++ b/src/backend/vlm.py
@@ -693,7 +693,7 @@ YOUR TASK:
 - categories: Pick from allowed list only. English. Array format.
 - tags: {"Keep all existing user tags AND add more from the visual analysis." if product_data else "Generate 10 relevant search tags."} English.
 - colors: Use visible product colors from ALLOWED COLORS only. English.
-{f"Keep any other fields from the existing data (price, SKU, etc.) unchanged." if product_data else ""}
+{"Keep any other fields from the existing data (price, SKU, etc.) unchanged." if product_data else ""}
 
 Return ONLY valid JSON. No markdown, no comments."""
 
diff --git a/tests/conftest.py b/tests/conftest.py
index 8ac22c5..3f9517d 100644
--- a/tests/conftest.py
+++ b/tests/conftest.py
@@ -19,14 +19,12 @@ Pytest configuration and shared fixtures for unit tests.
 This module provides reusable fixtures and mocks for testing
 the catalog enrichment backend without external dependencies.
 """
-import os
 import json
 import base64
 import pytest
 from io import BytesIO
 from PIL import Image
-from pathlib import Path
-from unittest.mock import Mock, AsyncMock, MagicMock
+from unittest.mock import Mock, AsyncMock
 from typing import Dict, Any
 
 
diff --git a/tests/test_config.py b/tests/test_config.py
index 5804b0f..1c0ada8 100644
--- a/tests/test_config.py
+++ b/tests/test_config.py
@@ -20,7 +20,6 @@ Tests Config class with various valid and invalid configurations.
 """
 import pytest
 import yaml
-from pathlib import Path
 from backend.config import Config, get_config
 
 
diff --git a/tests/test_image_utils.py b/tests/test_image_utils.py
index 240dd28..5720e1d 100644
--- a/tests/test_image_utils.py
+++ b/tests/test_image_utils.py
@@ -18,7 +18,6 @@ Unit tests for pure functions in image.py module.
 
 Tests image generation utility functions without external dependencies.
 """
-import pytest
 from backend.image import _render_flux_prompt, _extract_base64_image_from_flux_response
 
 
diff --git a/tests/test_product_manual_unit.py b/tests/test_product_manual_unit.py
index 6edc021..68cf52a 100644
--- a/tests/test_product_manual_unit.py
+++ b/tests/test_product_manual_unit.py
@@ -23,12 +23,11 @@ and knowledge extraction with mocked external dependencies.
 import json
 import numpy as np
 import pytest
-from unittest.mock import Mock, patch, MagicMock
+from unittest.mock import Mock, patch
 
 from backend.product_manual import (
     _chunk_text,
     _embed_texts,
-    _get_api_key,
     ProductManualContext,
     process_manual_pdf,
     generate_manual_queries,
@@ -90,7 +89,7 @@ class TestEmbedTextsBatching:
     @patch("backend.product_manual.get_config")
     @patch("backend.product_manual._get_api_key", return_value="test-key")
     def test_batches_large_inputs(self, mock_key, mock_config, mock_openai):
-        from backend.product_manual import _embed_texts, EMBEDDING_BATCH_SIZE
+        from backend.product_manual import EMBEDDING_BATCH_SIZE
 
         mock_cfg = Mock()
         mock_cfg.get_embeddings_config.return_value = {"url": "http://test:8005/v1", "model": "test-embed"}
@@ -119,7 +118,6 @@ class TestEmbedTextsBatching:
     @patch("backend.product_manual.get_config")
     @patch("backend.product_manual._get_api_key", return_value="test-key")
     def test_single_batch_for_small_inputs(self, mock_key, mock_config, mock_openai):
-        from backend.product_manual import _embed_texts
 
         mock_cfg = Mock()
         mock_cfg.get_embeddings_config.return_value = {"url": "http://test:8005/v1", "model": "test-embed"}
@@ -137,7 +135,7 @@ class TestEmbedTextsBatching:
     @patch("backend.product_manual.get_config")
     @patch("backend.product_manual._get_api_key", return_value="test-key")
     def test_limits_embedding_inputs_below_token_cap(self, mock_key, mock_config, mock_openai):
-        from backend.product_manual import _embed_texts, EMBEDDING_INPUT_MAX_WORDS
+        from backend.product_manual import EMBEDDING_INPUT_MAX_WORDS
 
         mock_cfg = Mock()
         mock_cfg.get_embeddings_config.return_value = {"url": "http://test:8005/v1", "model": "test-embed"}
@@ -606,7 +604,7 @@ class TestFaqGenerationWithManual:
         mock_openai.return_value.chat.completions.create.return_value = [mock_chunk]
 
         from backend.vlm import _call_nemotron_generate_faqs
-        result = _call_nemotron_generate_faqs(
+        _ = _call_nemotron_generate_faqs(
             {"title": "Handbag", "description": "Leather bag"},
             locale="en-US",
         )
diff --git a/tests/test_reflection_unit.py b/tests/test_reflection_unit.py
index 0a5efa7..bb2016e 100644
--- a/tests/test_reflection_unit.py
+++ b/tests/test_reflection_unit.py
@@ -19,7 +19,6 @@ Unit tests for image quality reflection module.
 Tests the reflection/evaluation system that compares original and generated images.
 """
 import json
-import pytest
 from unittest.mock import Mock, patch
 from backend.reflection import (
     evaluate_image_quality,
diff --git a/tests/test_utils.py b/tests/test_utils.py
index e60f077..6f13608 100644
--- a/tests/test_utils.py
+++ b/tests/test_utils.py
@@ -17,7 +17,6 @@
 Unit tests for the shared parse_llm_json utility.
 """
 import json
-import pytest
 from backend.utils import parse_llm_json
 
 
diff --git a/tests/test_vlm_unit.py b/tests/test_vlm_unit.py
index 2eaad74..6cba787 100644
--- a/tests/test_vlm_unit.py
+++ b/tests/test_vlm_unit.py
@@ -20,7 +20,7 @@ Tests VLM analysis, enhancement, and branding functions without external depende
 """
 import json
 import pytest
-from unittest.mock import Mock, patch, MagicMock
+from unittest.mock import Mock, patch
 from backend.vlm import (
     _call_vlm,
     _call_nemotron_structure_vlm,
@@ -1467,7 +1467,7 @@ class TestRunVLMAnalysis:
         mock_enhance.return_value = sample_vlm_response
         
         brand_instructions = "Use premium luxury tone"
-        result = run_vlm_analysis(sample_image_bytes, "image/png", "en-US", None, brand_instructions)
+        _ = run_vlm_analysis(sample_image_bytes, "image/png", "en-US", None, brand_instructions)
         
         # Should pass brand_instructions to enhance
         mock_enhance.assert_called_once()
